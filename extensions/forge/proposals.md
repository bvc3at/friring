# Forge proposals

Generated by the forge agent — review, then apply with:

```bash
~/.config/friring/extensions/forge/scripts/proposals.sh apply <slug>
```

_No open proposals yet. The forge-scan automation populates this on its next run
(or send `scan` to the forge session)._
