# Rust
/target
**/*.rs.bk
*.pdb

# Nix / direnv dev environment (flake.nix is committed; flake.lock is too —
# generate it with `nix flake lock`). The direnv cache is local-only.
.direnv/
# Persistent dev sandboxes live under /target (already ignored above):
#   target/dev-sandbox/<profile>/  — see scripts/dev/sandbox.sh

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.tarpaulin
cobertura.xml
junit.xml

# Nextest
.nextest/

# Cargo-pup
.pup/

# Insta snapshots (review and commit)
*.snap.new

# Environment
.env
.env.local

# Logs
*.log

# Temporary files
tmp/
temp/

# Node (website linting tools)
node_modules/

# Eleventy build output for the website
_site/
.claude/scheduled_tasks.lock
.aider*

# Fork (Friring): Claude Code project config is committed (commands, skills,
# path-scoped rules under .claude/rules/). Only machine-local settings and the
# local git worktrees stay ignored (.claude/scheduled_tasks.lock is above).
.claude/settings.local.json
.claude/worktrees/

# Local orchestrator/agent worker workspaces (stray nested checkouts)
thurbox/
friring/

# Python bytecode cache (from the packaging bump scripts)
__pycache__/
*.pyc
