---
paths:
  - "website/**"
---

# Website

- Install deps with the lockfile: `npm ci`.
- Lint and format via `npm run lint:website` / `npm run fmt:website` (Prettier +
  htmlhint + stylelint + eslint). See `docs/DEVELOPMENT.md`.
